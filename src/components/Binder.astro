<div class="container">
  <figure class="circle"></figure>
  <h1>1</h1>
  <p>Erhvervs- <br />erfaring</p>
</div>

<style>
  .container {
    position: relative;
    width: 12vw;
    height: auto;
    z-index: 10;
    transition: transform 0.2s;
    place-self: center;
    aspect-ratio: 1/1;
  }

  .container:hover {
    transform: scale(1.05);
    transition: transform 0.2s;
  }

  .circle {
    position: absolute;
    width: 3vw; /* fast bredde */
    height: 3vw; /* samme som bredde for cirkel */
    border: 2px solid black;
    border-radius: 50%;
    z-index: 5;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 0;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    transition: 0.3s;
  }

  .container:hover .circle {
    width: 8vw; /* fast bredde */
    height: 8vw; /* samme som bredde for cirkel */
    transition: 0.3s;
  }

  h1 {
    font-family: owners-xxwide, sans-serif;
    font-size: calc(2.2cqw * var(--font-scale));
    text-transform: uppercase;
    color: #000000;
    font-weight: normal;
    text-align: center;
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    opacity: 1;
  }

  p {
    font-family: owners-xxwide, sans-serif;
    font-size: calc(0.6cqw * var(--font-scale));
    text-transform: uppercase;
    color: #000000;
    font-weight: 600;
    text-align: center;
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    opacity: 0;
  }

  .container:hover h1 {
    opacity: 0;
  }

  .container:hover p {
    opacity: 1;
    transition: 0.6s;
  }
</style>
