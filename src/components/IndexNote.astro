<section class="note_outer" data-aos="fade-left" data-aos-duration="700">
  <div class="note_container">
    <h3>FIND RUNDT</h3>
    <h4 class="til">til:</h4>
    <h4 class="fra">note</h4>
    <h4 class="dato">dato:</h4>
    <div class="note-box">
      <div class="note-line note-line-1"></div>
      <div class="note-line note-line-2"></div>
      <div class="note-line note-line-3"></div>
    </div>
    <p class="note-text">
      På udkig efter noget? <br />
      Skyd genvej her
    </p>
    <div class="note-links">
      <a href="about" class="highlight">om mig</a>
      <a href="cv/experience" class="highlight">erhvervserfaring</a>
      <a href="cv/competencies" class="highlight">faglige kompetencer</a>
      <a href="cv/other-projects" class="highlight">øvrige projekter</a>
    </div>
  </div>
</section>

<script is:inline>
  document.querySelectorAll(".highlight").forEach((el) => {
    let lastDeg = null;
    el.addEventListener("mouseenter", () => {
      const choices = [-2, 1.5, 3.2];
      let deg;
      do {
        deg = choices[Math.floor(Math.random() * choices.length)];
      } while (deg === lastDeg && choices.length > 1);
      el.style.transform = `rotate(${deg}deg) scale(1.05)`;
      lastDeg = deg;
    });
    el.addEventListener("mouseleave", () => {
      el.style.transform = "";
    });
  });
</script>

<style>
  /* Note sektion */
  .note_outer {
    position: relative;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  /* Note container - fylder hele parent */
  .note_container {
    position: relative;
    width: 100%;
    height: 100%;
    display: flex;
    justify-content: center;
    align-items: center;
    background-image: url("/assets/yellow_paper.webp");
    background-size: contain;
    background-repeat: no-repeat;
    background-position: center;
    filter: drop-shadow(4px 6px 10px rgba(0, 0, 0, 0.25)) brightness(98%);
    aspect-ratio: 1/1;
  }

  h3 {
    position: absolute;
    top: 20%;
    left: 50%;
    transform: translate(-50%, -50%);
    font-family: owners-wide, sans-serif;
    font-weight: 500;
    -webkit-text-stroke: 1px black;
    font-size: 2cqw;
    z-index: 10;
  }

  .note-box {
    width: 60%;
    height: 10%;
    position: absolute;
    top: 33%;
    left: 50%;
    transform: translate(-50%, -50%);
  }

  .note-line-1 {
    width: 100%;
    height: 1px;
    background-color: black;
    position: absolute;
    z-index: 10;
    top: 0;
    left: 50%;
    transform: translate(-50%, -50%);
  }

  .note-line-2 {
    width: 100%;
    height: 1px;
    background-color: black;
    position: absolute;
    z-index: 10;
    top: 100%;
    left: 50%;
    transform: translate(-50%, -50%);
  }

  .note-line-3 {
    width: 1px;
    height: 100%;
    background-color: black;
    position: absolute;
    z-index: 10;
    top: 50%;
    left: 68%;
    transform: translate(-50%, -50%);
  }

  h4 {
    position: absolute;
    font-family: "Times New Roman", serif;
    font-size: 1cqw;
    transform: translate(-50%, -50%);
    font-weight: 500;
    z-index: 10;
    font-style: italic;
  }

  .til {
    top: 32%;
    left: 23%;
  }
  .fra {
    top: 42%;
    left: 23%;
  }
  .dato {
    top: 32%;
    left: 68%;
  }

  .note-text {
    position: absolute;
    top: 55%;
    left: 50%;
    transform: translate(-50%, -50%);
    font-family: "owners-wide", sans-serif;
    font-size: 1.2cqw;
    text-align: center;
    z-index: 10;
    color: black;
    text-transform: uppercase;
    text-wrap: nowrap;
  }

  .note-links {
    position: absolute;
    top: 80%;
    left: 50%;
    transform: translate(-50%, -50%);
    font-family: "owners-wide", sans-serif;
    font-size: 1.2cqw;
    text-align: center;
    z-index: 10;
    color: #2786af;
    text-transform: uppercase;
    display: flex;
    flex-direction: column;
    gap: 0.5vh;
    text-wrap: nowrap;
  }

  a {
    color: #2786af;
    text-decoration: none;
    transition: all 0.3s ease;
  }

  a:hover {
    text-decoration: underline;
  }

  @media (max-width: 800px) {
    h3 {
      font-size: 150%;
      text-wrap: nowrap;
    }

    h4 {
      font-size: 90%;
    }

    .note-text {
      display: none;
    }

    .note-links {
      font-size: 110%;
      top: 68%;
    }
  }
</style>
